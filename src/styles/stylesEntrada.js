import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    input: {
        borderWidth: 1,
        borderColor: '#ccc',
        padding: 8,
        marginBottom: 12,
        borderRadius: 4,
        marginTop: 10,
    },
    dataHora: {
        marginBottom: 12,
        fontStyle: 'italic',
        textAlign: 'center',
    },
});
