import { StyleSheet } from 'react-native';

export default StyleSheet.create({
    item: {
        backgroundColor: '#f1f1f1',
        padding: 12,
        marginBottom: 10,
        borderRadius: 8,
    },
    title: {
        fontWeight: 'bold',
        fontSize: 16,
    },
    empty: {
        textAlign: 'center',
        marginTop: 20,
        fontStyle: 'italic',
    },
});
